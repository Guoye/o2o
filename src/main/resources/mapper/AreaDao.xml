<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhangguoye.o2o.dao.AreaDao">
	<select id="queryArea" resultType="com.zhangguoye.o2o.entity.Area">
		select * 
		from tb_area
		order by priority
		desc
	</select>
	<select id="queryAreaById" resultType="com.zhangguoye.o2o.entity.Area">
		select *
		frome tb_area
		where area_id=#{id}
	</select>
	<insert id="insertArea" parameterType="com.zhangguoye.o2o.entity.Area">
		insert into tb_area(area_id, area_name, area_desc, priority, create_time, last_edit_time)
		value(
			#{areaId},
			#{area_name},
			#{area_desc},
			#{priority},
			#{create_time},
			#{last_edit_time}
		)
	</insert>
</mapper>        